-- A. Which _CURR are materialized (the real bug surface)
SELECT TABLE_NAME, TABLE_TYPE
FROM PROD_DV.INFORMATION_SCHEMA.TABLES
WHERE TABLE_SCHEMA='BUSINESS_VAULT'
  AND TABLE_NAME LIKE '%_CURR'
  AND TABLE_TYPE='BASE TABLE';

-- B. Is CURR really deduping? (settles Flag 2)
SELECT COUNT(*) AS total,
       COUNT(DISTINCT H_HK_AGENT_HUB) AS distinct_keys
FROM PROD_DV.RAW_VAULT.S_ING_AGENT_GENERAL_AGENT_AGENTCOMMISSIONBALANCETABMC_LDS;

-- C. Structure of the PK reference table
SELECT COLUMN_NAME, DATA_TYPE
FROM PROD_DV.INFORMATION_SCHEMA.COLUMNS
WHERE TABLE_NAME='SRC_TGT_RECONCILIATION';

-- D. Landing views with NO usable timestamp
SELECT TABLE_NAME FROM PROD_DV.INFORMATION_SCHEMA.TABLES
WHERE TABLE_SCHEMA='LANDING_ING'
EXCEPT
SELECT DISTINCT TABLE_NAME FROM PROD_DV.INFORMATION_SCHEMA.COLUMNS
WHERE TABLE_SCHEMA='LANDING_ING'
  AND COLUMN_NAME IN ('PREV_UPDT_TS','FILE_DATE','DV_LOAD_TIMESTAMP','LOAD_TIMESTAMP');
